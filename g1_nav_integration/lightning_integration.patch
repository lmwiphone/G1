--- a/src/core/system/slam.cc
+++ b/src/core/system/slam.cc
@@ -1,3 +1,4 @@
+#include "core/lightning_math.hpp"
 //
 // Created by xiang on 25-5-6.
 //
@@ -83,6 +84,7 @@
 
         /// subscribers
         node_ = std::make_shared<rclcpp::Node>("lightning_slam");
+        nav_mapping_pose_pub_ = node_->create_publisher<geometry_msgs::msg::PoseStamped>("/lightning/map_pose", 10);
 
         imu_topic_ = yaml["common"]["imu_topic"].as<std::string>();
         cloud_topic_ = yaml["common"]["lidar_topic"].as<std::string>();
@@ -211,7 +213,7 @@
             emitter << YAML::Key << "mode" << YAML::Value << "trinary";
             emitter << YAML::Key << "width" << YAML::Value << map.info.width;
             emitter << YAML::Key << "height" << YAML::Value << map.info.height;
-            emitter << YAML::Key << "resolution" << YAML::Value << float(0.05);
+            emitter << YAML::Key << "resolution" << YAML::Value << map.info.resolution;
             std::vector<double> orig{map.info.origin.position.x, map.info.origin.position.y, 0};
             emitter << YAML::Key << "origin" << YAML::Value << orig;
             emitter << YAML::Key << "negate" << YAML::Value << 0;
@@ -236,6 +238,21 @@
         return;
     }
     lio_->ProcessIMU(imu);
+    const auto state = lio_->GetIMUState();
+    if (nav_mapping_pose_pub_ && state.pose_is_ok_) {
+        geometry_msgs::msg::PoseStamped pose;
+        pose.header.frame_id = "map";
+        pose.header.stamp = math::FromSec(state.timestamp_);
+        pose.pose.position.x = state.pos_.x();
+        pose.pose.position.y = state.pos_.y();
+        pose.pose.position.z = state.pos_.z();
+        const auto q = state.rot_.unit_quaternion();
+        pose.pose.orientation.x = q.x();
+        pose.pose.orientation.y = q.y();
+        pose.pose.orientation.z = q.z();
+        pose.pose.orientation.w = q.w();
+        nav_mapping_pose_pub_->publish(pose);
+    }
 }
 
 void SlamSystem::ProcessLidar(const sensor_msgs::msg::PointCloud2::SharedPtr& cloud) {
--- a/src/core/system/slam.h
+++ b/src/core/system/slam.h
@@ -5,6 +5,7 @@
 #ifndef LIGHTNING_SLAM_H
 #define LIGHTNING_SLAM_H
 
+#include <geometry_msgs/msg/pose_stamped.hpp>
 #include <rclcpp/rclcpp.hpp>
 #include <sensor_msgs/msg/imu.hpp>
 #include <sensor_msgs/msg/point_cloud2.hpp>
@@ -94,6 +95,7 @@
 
     /// 实时模式下的ros2 node, subscribers
     rclcpp::Node::SharedPtr node_;
+    rclcpp::Publisher<geometry_msgs::msg::PoseStamped>::SharedPtr nav_mapping_pose_pub_;
     std::string imu_topic_;
     std::string cloud_topic_;
     std::string livox_topic_;
--- a/src/core/system/loc_system.cc
+++ b/src/core/system/loc_system.cc
@@ -1,3 +1,4 @@
+#include "core/lightning_math.hpp"
 //
 // Created by xiang on 25-9-12.
 //
@@ -57,11 +58,57 @@
             Timer::Evaluate([&]() { ProcessLidar(cloud); }, "Proc Lidar", true);
         });
 
+    options_.pub_tf_ = node_->declare_parameter<bool>("publish_tf", false);
+    nav_global_pub_ = node_->create_publisher<geometry_msgs::msg::PoseStamped>("/lightning/map_pose", 10);
+    nav_lio_pub_ = node_->create_publisher<geometry_msgs::msg::PoseStamped>("/lightning/lio_pose", 10);
     if (options_.pub_tf_) {
         tf_broadcaster_ = std::make_shared<tf2_ros::TransformBroadcaster>(node_);
-        loc_->SetTFCallback(
-            [this](const geometry_msgs::msg::TransformStamped &pose) { tf_broadcaster_->sendTransform(pose); });
     }
+    loc_->SetTFCallback([this](const geometry_msgs::msg::TransformStamped &input) {
+        geometry_msgs::msg::PoseStamped pose;
+        pose.header = input.header;
+        pose.pose.position.x = input.transform.translation.x;
+        pose.pose.position.y = input.transform.translation.y;
+        pose.pose.position.z = input.transform.translation.z;
+        pose.pose.orientation = input.transform.rotation;
+        nav_global_pub_->publish(pose);
+        if (tf_broadcaster_) {
+            auto tf = input;
+            tf.child_frame_id = "lightning_tracking";
+            tf_broadcaster_->sendTransform(tf);
+        }
+    });
+    loc_->SetNavLioCallback([this](const NavState& state) {
+        geometry_msgs::msg::PoseStamped pose;
+        pose.header.frame_id = "lightning_odom";
+        pose.header.stamp = math::FromSec(state.timestamp_);
+        pose.pose.position.x = state.pos_.x();
+        pose.pose.position.y = state.pos_.y();
+        pose.pose.position.z = state.pos_.z();
+        const auto q = state.rot_.unit_quaternion();
+        pose.pose.orientation.x = q.x();
+        pose.pose.orientation.y = q.y();
+        pose.pose.orientation.z = q.z();
+        pose.pose.orientation.w = q.w();
+        nav_lio_pub_->publish(pose);
+    });
+    nav_initial_sub_ = node_->create_subscription<geometry_msgs::msg::PoseWithCovarianceStamped>(
+        "/lightning/initialpose", 10,
+        [this](geometry_msgs::msg::PoseWithCovarianceStamped::SharedPtr msg) {
+            if (msg->header.frame_id != "map") {
+                RCLCPP_WARN(node_->get_logger(), "Initial pose must be expressed in map");
+                return;
+            }
+            const auto& p = msg->pose.pose;
+            Eigen::Quaterniond q(p.orientation.w, p.orientation.x, p.orientation.y, p.orientation.z);
+            const Vec3d t(p.position.x, p.position.y, p.position.z);
+            if (!q.coeffs().allFinite() || q.norm() < 1e-8 || !t.allFinite()) {
+                RCLCPP_WARN(node_->get_logger(), "Rejecting invalid initial pose");
+                return;
+            }
+            q.normalize();
+            SetInitPose(SE3(q, t));
+        });
 
     bool ret = loc_->Init(yaml_path, map_path);
     if (ret) {
--- a/src/core/system/loc_system.h
+++ b/src/core/system/loc_system.h
@@ -6,6 +6,8 @@
 #define LIGHTNING_LOC_SYSTEM_H
 
 #include <tf2_ros/transform_broadcaster.h>
+#include <geometry_msgs/msg/pose_stamped.hpp>
+#include <geometry_msgs/msg/pose_with_covariance_stamped.hpp>
 #include <rclcpp/rclcpp.hpp>
 #include <sensor_msgs/msg/imu.hpp>
 #include <sensor_msgs/msg/point_cloud2.hpp>
@@ -57,6 +59,9 @@
 
     /// 实时模式下的ros2 node, subscribers
     rclcpp::Node::SharedPtr node_;
+    rclcpp::Publisher<geometry_msgs::msg::PoseStamped>::SharedPtr nav_global_pub_;
+    rclcpp::Publisher<geometry_msgs::msg::PoseStamped>::SharedPtr nav_lio_pub_;
+    rclcpp::Subscription<geometry_msgs::msg::PoseWithCovarianceStamped>::SharedPtr nav_initial_sub_;
     std::shared_ptr<tf2_ros::TransformBroadcaster> tf_broadcaster_ = nullptr;
 
     std::string imu_topic_;
--- a/src/core/localization/localization.cpp
+++ b/src/core/localization/localization.cpp
@@ -276,6 +276,9 @@
     if (!dr_state.pose_is_ok_) {
         return;
     }
+    if (nav_lio_callback_) {
+        nav_lio_callback_(dr_state);
+    }
 
     // /// 停车判定
     // constexpr auto kThVbrbStill = 0.05;  // 0.08;
--- a/src/core/localization/localization.h
+++ b/src/core/localization/localization.h
@@ -81,6 +81,8 @@
     using PointcloudWorldCallback = std::function<void(const sensor_msgs::msg::PointCloud2& pointcloud)>;
 
     void SetTFCallback(TFCallback&& callback);
+    using NavLioCallback = std::function<void(const NavState&)>;
+    void SetNavLioCallback(NavLioCallback callback) { nav_lio_callback_ = std::move(callback); }
 
     // void SetPathCallback(std::function<void(const nav_msgs::msg::Path& path)>&& callback);
     // void SetPointcloudWorldCallback(std::function<void(const sensor_msgs::msg::PointCloud2& pointcloud)>&& callback);
@@ -118,6 +120,7 @@
 
     /// 框架相关
     TFCallback tf_callback_;
+    NavLioCallback nav_lio_callback_;
     LocStateCallback loc_state_callback_;
     PointcloudBodyCallback pointcloud_body_callback_;
     PointcloudWorldCallback pointcloud_world_callback_;
--- a/src/app/run_slam_online.cc
+++ b/src/app/run_slam_online.cc
@@ -1,3 +1,6 @@
+#include <vector>
+#include <string>
+#include <rclcpp/utilities.hpp>
 //
 // Created by xiang on 25-3-18.
 //
@@ -17,12 +20,20 @@
     google::InitGoogleLogging(argv[0]);
     FLAGS_colorlogtostderr = true;
     FLAGS_stderrthreshold = google::INFO;
-    google::ParseCommandLineFlags(&argc, &argv, true);
+    rclcpp::init(argc, argv);
+    // Strip ROS arguments before gflags sees --ros-args/--params-file.
+    auto non_ros_args = rclcpp::remove_ros_arguments(argc, argv);
+    std::vector<char*> clean_argv;
+    for (auto& arg : non_ros_args) clean_argv.push_back(arg.data());
+    int clean_argc = static_cast<int>(clean_argv.size());
+    clean_argv.push_back(nullptr);
+    char** clean_argv_ptr = clean_argv.data();
+    google::ParseCommandLineFlags(&clean_argc, &clean_argv_ptr, true);
 
     using namespace lightning;
 
     /// 需要rclcpp::init
-    rclcpp::init(argc, argv);
+
 
     SlamSystem::Options options;
     options.online_mode_ = true;
--- a/src/app/run_loc_online.cc
+++ b/src/app/run_loc_online.cc
@@ -1,3 +1,6 @@
+#include <vector>
+#include <string>
+#include <rclcpp/utilities.hpp>
 //
 // Created by xiang on 25-3-18.
 //
@@ -17,10 +20,18 @@
     FLAGS_colorlogtostderr = true;
     FLAGS_stderrthreshold = google::INFO;
 
-    google::ParseCommandLineFlags(&argc, &argv, true);
+    rclcpp::init(argc, argv);
+    // Strip ROS arguments before gflags sees --ros-args/--params-file.
+    auto non_ros_args = rclcpp::remove_ros_arguments(argc, argv);
+    std::vector<char*> clean_argv;
+    for (auto& arg : non_ros_args) clean_argv.push_back(arg.data());
+    int clean_argc = static_cast<int>(clean_argv.size());
+    clean_argv.push_back(nullptr);
+    char** clean_argv_ptr = clean_argv.data();
+    google::ParseCommandLineFlags(&clean_argc, &clean_argv_ptr, true);
     using namespace lightning;
 
-    rclcpp::init(argc, argv);
+
 
     LocSystem::Options opt;
     LocSystem loc(opt);
